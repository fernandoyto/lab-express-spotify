<div class="container">
  <div class="row">
    {{#each albumsArray}}
      <div class="card">
        <img src="{{images.0.url}}" alt="">
        <div class="card-body">
          <h5>{{name}}</h5>
          <a href="/tracks/{{id}}">View Tracks</a>
        </div>
      </div>
    {{/each}}
  </div>
</div>